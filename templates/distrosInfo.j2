#!/bin/bash
#distros and the official names of the images must be in a one to one correspondance!

declare -A distros_target     # Create an associative array

# distros
distros_target[centos7]="centos7"
distros_target[debianWheezy]="debian"
distros_target[ubuntu]="ubuntu"
distros_target[ubuntuxenial]="ubuntu"
distros_target[fedora]="fedora"
distros_target[debianJessie]="debian"
distros_target[rstudio]="rstudio ubuntu"
distros_target[zeppelin]="zeppelin ubuntu"

